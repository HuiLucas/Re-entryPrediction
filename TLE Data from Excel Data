import pandas as pd

# Load spreadsheet
xl = pd.ExcelFile('path_to_your_file.xlsx')

# Load a sheet into a DataFrame by its name
df = xl.parse('Sheet1')

# Print the column data for every line
for index, row in df.iterrows():